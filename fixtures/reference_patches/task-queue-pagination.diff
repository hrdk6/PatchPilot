diff --git a/task_queue/pagination.py b/task_queue/pagination.py
--- a/task_queue/pagination.py
+++ b/task_queue/pagination.py
@@ -38,6 +38,6 @@
         raise ValueError("per_page must be positive")
     if page < 1:
         raise ValueError("page is 1-indexed")
-    start = page * per_page
+    start = (page - 1) * per_page
     end = start + per_page
     return Page(items=list(items[start:end]), page=page, per_page=per_page, total=len(items))
