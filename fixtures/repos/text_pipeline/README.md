# text_pipeline

Word-frequency analytics. `text_pipeline/tokenizer.py` turns raw text into
tokens; `text_pipeline/analytics.py` counts them and reports the most common.

```bash
python -m pytest -q
```
