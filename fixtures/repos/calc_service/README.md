# calc_service

A small reporting service: arithmetic helpers in `calc_service/operations.py`,
report rendering in `calc_service/reporting.py`, pytest suite in `tests/`.

```bash
python -m pytest -q
```
